spring:
   kafka:
      consumer:
         auto-offset-reset: earliest
         group-id: taketoday
test:
   topic: embedded-test-topic

monitor:
   kafka:
      bootstrap:
         config: "PLAINTEXT://localhost:9085"
      consumer:
         groupid: "taketodayGrp"